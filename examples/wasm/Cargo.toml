[package]
name = "counter-demo-rs"
version = "0.1.0"
edition = "2021"

# This table keeps the example outside the engine workspace.
[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
worker = "0.8.5"
serde_json = "1"
